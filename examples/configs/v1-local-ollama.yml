provider: ollama

providers:
  ollama:
    base_url: "http://localhost:11434/v1"
    model: "llama3"
    max_tokens: 1024
    timeout: 60s

review:
  strictness: normal
  passes: 1
  filter_mode: added
  mr_diff_source: auto

debug: false
stream: true

